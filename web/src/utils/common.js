export const urls = {
  home: "/",
  test: "/test",
};

export const navbar = ["entropy", "about", "features", "contact"];

export const text = {
  homePage: {
    info: `Entropy Terminal Task Manager is a tool similar to a typical task
    manager, but it is faster and more reliable because it relies on the terminal. 
    
    This is especially useful when every second counts. Let's
    take a look at some of Entropy's features.`,
  },
};
